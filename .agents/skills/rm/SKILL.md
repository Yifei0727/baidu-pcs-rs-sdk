---
name: baidu-pan-rm
description: 删除百度网盘中的一个或多个文件/目录，支持批量删除和递归删除。当用户需要清理、删除网盘上的文件或目录时激活此技能。
---

# 百度网盘远程文件删除

## 使用场景

- 删除网盘中不再需要的文件
- 批量删除多个文件或目录
- 清空某个远程目录

## 命令格式

```bash
baidu-pcs-cli-rs rm <远程路径>... [-r]
# 别名
baidu-pcs-cli-rs del <远程路径>...
baidu-pcs-cli-rs remove <远程路径>...
```

## 参数说明

| 参数 | 类型 | 说明 | 示例 |
|------|------|------|------|
| `<远程路径>...` | 必填（支持多个） | 要删除的网盘文件/目录路径，空格分隔 | `/文件1 /文件2` |
| `-r` / `--recursive` | 可选 | 递归删除子目录及其内容 | `-r` |

## 注意事项

- **删除操作不可恢复**，请在执行前确认路径正确
- 支持同时传入多个路径批量删除
- 删除目录时建议加 `-r` 参数确保子内容一并删除

## 示例

```bash
# 删除单个文件
baidu-pcs-cli-rs rm /临时/old-file.txt

# 批量删除多个文件
baidu-pcs-cli-rs rm /临时/a.txt /临时/b.txt /临时/c.txt

# 递归删除目录
baidu-pcs-cli-rs rm /废弃项目 -r
```
